<?xml version="1.0" encoding="utf-8"?>
<!-- Declares the home-screen playback widget. Its transport buttons are
     service PendingIntents handled by PlaybackService.onStartCommand (a
     BroadcastReceiver cannot bind to a service on modern Android), so no
     action receiver is needed. Merges into the app manifest at build time. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <receiver
            android:name=".PlaybackWidgetProvider"
            android:exported="false"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/playback_widget_info" />
        </receiver>
    </application>

</manifest>
