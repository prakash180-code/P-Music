<?xml version="1.0" encoding="utf-8"?>
<!-- App Widget metadata: 4x1 horizontal playback widget.
     updatePeriodMillis is 0 because the provider self-updates from the
     MediaController (state events + a progress ticker) instead of waking
     on a system schedule. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_description"
    android:initialLayout="@layout/widget_playback"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:resizeMode="horizontal|vertical"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
